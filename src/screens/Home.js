import React from 'react';
import {
  View,
  Text,
  SafeAreaView,
  ScrollView,
  ImageBackground,
} from 'react-native';

const Home = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Hello Test</Text>
        <ImageBackground></ImageBackground>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Home;
